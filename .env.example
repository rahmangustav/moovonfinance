# Wajib untuk telegram_bot.py (kontrol pipeline dari luar sesi Claude Code)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Opsional — voice ElevenLabs (core/tts.py). Kalau kosong, otomatis fallback
# ke edge-tts (id-ID-GadisNeural), jadi tidak wajib diisi.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
ELEVENLABS_MODEL=

# Auth YouTube (upload/analisis channel) TIDAK lewat env var — pakai file OAuth
# di config/client_secrets.json + config/token.json (lihat core/youtube_uploader.py).
